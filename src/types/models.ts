export interface Cause {
	id: string;
	title: string;
	description: string;
	image: string;
}

export interface Member {
	id: string;
	name: string;
	role: string;
	image: string;
}

export interface GalleryItem {
	id: string;
	url: string;
	caption: string;
}
